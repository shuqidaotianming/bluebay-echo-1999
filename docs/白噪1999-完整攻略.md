# 《白噪 · 1999 蓝湾镇回声事件》完整攻略（重度版 · 含剧透）

> 60 页 · 3 道密码题 · 8 结局。结局由**你解出并掌握的线索**决定；不解题，你只能"放弃"或"冤枉好人"。

---

## 0. 机制

- 打开一个页面＝记一条线索（`sessionStorage`，同会话有效，重开清空）。
- **结案＝指认嫌疑人**（聊天里的记者"明月如霜"）。你能指谁、指得对不对，取决于你凑齐了哪几条能互相咬合的证据。
- **两道锁（保险箱、机房）是硬门槛**：6 条核心线索只锁在文件柜里，而开锁要先解密码题。断锁测试：不解锁，7/7 核心线索不可达。
- 三把口令**不再有任何明文**，全部要解：凯撒 / Base64 / 摩尔斯。另有一层隐写（十六进制）通往隐藏结局。

## 1. 三道密码题（答案 · 剧透）

| 锁 | 谜题 | 线索来源页 | 解法 | 答案 |
| --- | --- | --- | --- | --- |
| 保险箱 | 凯撒·退三格 | 桌面「磁带标签」(搜 标签/密文/磁带) | `WLGH` 每字母退回 3 位（W→T） | `tide` |
| 机房 BIOS | Base64 | 「工程便签」(搜 便签/工程/编码) | 控制台 `atob("Y2hhb3NoZW5nOQ==")` | `chaosheng9` |
| 警务内网 | 摩尔斯 | 「接线员的口哨」(搜 接线员/口哨/摩斯) | `.-.. .-- .---- ----. ----. ----.` 查表 | `lw1999` |
| 隐藏层 | 十六进制隐写 | 保险箱内「被抹去波形的带子」 | 选中被遮蔽的 `7374696c6c68657265`→hex 转字符 | `stillhere`（再拿去搜索） |

> 保险箱认字母不认数字；`99.4` 只是电台频率，不是口令（旧版误导点已清除）。

## 2. 真结局最短路线

1. 桌面 → 读「未寄出的信」→ 打开「一张磁带标签」→ 解凯撒得 `tide`。
2. 桌面 → 「保险箱」输 `tide` → 进文件柜，逐页读完：`潮声第九夜·录音稿`、`沈砚日记（后半）`、`瀚洋7号·名单`（3 条核心线索）。顺手点开「被抹去波形的带子」（为隐藏结局备料）。
3. 回桌面 → 搜「便签」→ 打开「工程便签」→ 控制台 `atob(...)` 得 `chaosheng9`。
4. 「机房 BIOS」输 `chaosheng9` → 文件柜读完：`白噪计划·备忘录`、`对广播站资助协议`、`延盛港监控`、`播音室监控`（再 3+1 条核心线索）。
5. 补齐资金/词条：搜「瀚洋」读专访、搜「听潮会」「次声波」看维基词条。
6. 桌面 → 聊天 → 「明月如霜」→ 此时最重的选项**「指认真凶——总工程师柯振」**浮现 → 点它 → **真结局**。

## 3. 隐藏结局（潮声未绝）

在第 2 步点开「被抹去波形的带子」→ 选中被遮蔽的十六进制 → 解出 `stillhere` → 去**千禧搜索输入 `stillhere`** → 打开「浮标信标·仍在闪」→ 再回「明月如霜」，会多出**「追问那个仍在闪的浮标信标」**选项 → 隐藏结局。

## 4. 八个结局 · 解锁条件

| 结局 | 触发（指认/提交） | 需要的线索 |
| --- | --- | --- |
| 官方结论 | 放弃深究 | 无（开局即可） |
| 误判（坏） | 指认版主"守夜人" | 无（证据不全时的陷阱） |
| 一知半解 | 只证同类海难 | `news_1999`＋`doc_wreck_report` |
| 指认瀚洋 | 指控韩启昌 | `news_hanyang`＋`doc_subsidy`＋`wiki_tingchao` |
| 频率之下 | 公开次声波实验 | `doc_experiment`＋`transcript_tape`＋`wiki_frequency` |
| 第九夜 | 还原灯塔之夜 | `surv_port`＋`surv_studio`＋`diary_shenyan2` |
| **真结局** | **指认真凶柯振** | 6 条核心线索（需解凯撒+Base64） |
| **隐藏结局** | 追问浮标信标 | 真结局 6 条 ＋ `clue_buoy`（需再解隐写） |

> 关键点：不解锁，只有"官方结论 / 误判"两项可选——你无法凭猜测到达真结局。

## 5. 搜索关键词表（节选，其余需自己想到）

- 公开可搜：1999/海难、失踪、沈砚/潮声、瀚洋/韩启昌、灯塔、停播、人墙/渔港、福利院、听潮会、次声波/4.5hz、调查报告/海事、日记、检修/台呼、报案、举报。
- 引到锁前（不再直达）：名单/第九夜/录音→保险箱；白噪计划/资助/监控/柯振→机房。
- 密码题入口：标签/密文/磁带→磁带标签；便签/工程/编码/base64→工程便签；接线员/口哨/摩斯→接线员的口哨。
- 隐藏：`stillhere`/浮标/信标→浮标信标。

## 6. 全 60 页清单

桌面1 / 聊天1 / 搜索1 / 登录3 / 文件柜2 / 索引枢纽2 / 浏览40（信·日记·论坛帖8·新闻8·维基5·卷宗6·日志录音2·监控2·密码题与隐藏层5）/ 结局8。

## 7. 卡点

- **进不去锁？** 先解对应密码题（第 1 节），别试 99.4。
- **搜不到某词？** 说明它要么该去锁里拿，要么得你自己从人名/编号/暗号里想到。
- **只有"放弃/误判"能选？** 你还没解锁取到核心线索——去解凯撒和 Base64。
- **换结局？** 结局页是终点，需"重开"新会话重新收集。
